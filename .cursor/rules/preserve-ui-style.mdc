---
description: Fusion UI 已定稿；改动须符合 DESIGN.md，禁止随手换风格
alwaysApply: true
---

# UI 风格（已定稿）

单一事实来源：**仓库根目录 `DESIGN.md`**。修改前端时先对照该文件；**除非用户当次对话明确要求改版**，否则不得更换配色体系、版心宽度规则、圆角尺度或字体方案。

## 当前规范摘要

- **字体**：全站 `Noto Sans SC`（`src/app/layout.tsx`），勿改回依赖系统默认中文 fallback。
- **首页**：`max-w-6xl` 居中；未登录仅登录/注册门闩；已注册登录后三卡并列；`neutral-50` 底、白卡片、`rounded-2xl`，主标题 **无渐变字**（详见 `DESIGN.md`）。
- **内页顶栏**：`TopNav` 保持简洁；与 `DESIGN.md` 中「按钮/输入」形态一致即可。
- **禁止**：为「更好看」批量改 class、引入新主色或新圆角体系、恢复渐变标题或杂乱双栏营销布局。

## 例外

仅当用户**明确说**要改版（例如「换深色主题」）时，可偏离；改版后应同步更新 `DESIGN.md`。
